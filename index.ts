//Excercise 18

//let places: string[] = ['Italy', 'Germany', 'Eifel tower', 'Times square'];

// print your array in its original order.
//console.log('original:', places);

// print your array in alphabetical order without modifying the actual list.
//console.log('copy (alphabetical):', [...places].sort());

// show that your array is still in its original order by printing it.
//console.log('original:', places);

// print your array in reverse alphabetical order without changing the order of the original list
//console.log('copy (reverse alphabetical):', [...places].sort().reverse());

// show that your array is still in its original order by printing it again.
//console.log('original:', places);

// reverse the order of your list. Print the array to show that its order has changed.
//console.log('original (reversed):', places.reverse());

// reverse the order of your list again. Print the list to show it's back to its original order.
//console.log('original:', places.reverse());

// sort your array to its stored in alphabetical order. Print the array to show that it's order has been changed.
//console.log('original (alphabetical):', places.sort());

// sort to change your array to its stored in reverse alphabetical order. Print the list to show that its order has changed.
//console.log('original (reverse alphabetical):', places.sort().reverse());




